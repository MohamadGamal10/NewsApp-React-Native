import { Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

export default function FavArticles() {
    return (
        <SafeAreaView>
            <Text>FavArticles</Text>
        </SafeAreaView>
    )
}